import java.util.*;
import java.io.*;

public class Benford {
	private static List<Integer> benfordList;
	
	public Benford(){
		benfordList = new ArrayList<Integer>();
	}
	
	public static void readCounts(String inputFile){
		try {
			Scanner fileScanner = new Scanner(new File (inputFile));
			System.out.print("Loading counts... ");
			while(fileScanner.hasNext())
				//System.out.println((int)fileScanner.next().charAt(0));
				if((int)fileScanner.next().charAt(0)>48 && (int)fileScanner.next().charAt(0)<=57)
					benfordList.add((int)fileScanner.next().charAt(0)); // Not currently working...
					
				else
					fileScanner.next();
			System.out.println(" done.");
			//for(String data:benfordList)
				
		} catch (FileNotFoundException e) {
			System.out.println("File not found, please check pathway and try again.");
		}
		
			
		}
	

}
